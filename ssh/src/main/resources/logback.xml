<?xml version="1.0" encoding="UTF-8" ?>

<configuration debug="false">

	<!-- 控制台 -->
	<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %logger - %msg%n</pattern>
		</encoder>
	</appender>
	<!-- 出错日志 appender  -->
	<appender name="ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- 按天回滚 daily -->
			<!-- log.dir 在maven profile里配置 -->
			<fileNamePattern>/tmp/es/error-%d{yyyy-MM-dd}.log</fileNamePattern>
			<!-- 日志最大的历史 60天 -->
			<maxHistory>60</maxHistory>
		</rollingPolicy>
		<encoder>
			<pattern>[%-5level] %d{HH:mm:ss.SSS} [%thread] %logger - %msg%n</pattern>
		</encoder>
	</appender>
	<!-- 访问日志 appender  -->
	<appender name="ACCESS" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- 按天回滚 daily -->
			<fileNamePattern>/tmp/es/access-%d{yyyy-MM-dd}.log</fileNamePattern>
			<!-- 日志最大的历史 60天 -->
			<maxHistory>60</maxHistory>
		</rollingPolicy>
		<encoder>
			<pattern>[%-5level] %d{HH:mm:ss.SSS} [%thread] %logger - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- 访问日志 appender  -->
	<appender name="SYS-USER" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- 按天回滚 daily -->
			<fileNamePattern>/tmp/es/sys-user-%d{yyyy-MM-dd}.log</fileNamePattern>
			<!-- 日志最大的历史 60天 -->
			<maxHistory>60</maxHistory>
		</rollingPolicy>
		<encoder>
			<pattern>[%-5level] %d{HH:mm:ss.SSS} [%thread] %logger - %msg%n</pattern>
		</encoder>
	</appender>

	<appender name="org.springframework" level="debug"/>

	<logger name="org.apache" level="debug">
		<appender-ref ref="CONSOLE"/>
	</logger>

	<!--错误日志-->
	<logger name="es-error" level="info">
		<appender-ref ref="ERROR"/>
	</logger>
	<!--访问日志-->
	<logger name="es-access" level="info">
		<appender-ref ref="ACCESS"/>
	</logger>
	<!--系统用户操作日志-->
	<logger name="es-sys-user" level="info">
		<appender-ref ref="SYS-USER"/>
	</logger>

	<!-- cache -->
	<logger name="com.sishuok.es.extra.aop" level="info">
		<appender-ref ref="CONSOLE"/>
	</logger>

	<root>
		<level value="debug" />
		<appender-ref ref="RollingFile" />
		<appender-ref ref="CONSOLE" />
	</root>
</configuration>



